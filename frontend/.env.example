# KIZUNA AI Frontend Environment Configuration
# Set NEXT_PUBLIC_DEMO_MODE=true for self-contained Vercel showcase deployment (no backend required)
NEXT_PUBLIC_DEMO_MODE=true

# Optional: Real backend URL (only used when NEXT_PUBLIC_DEMO_MODE=false)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME="KIZUNA AI"
